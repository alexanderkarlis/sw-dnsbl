package middleware

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestMiddleWare(t *testing.T) {
	assert.Equal(t, true, true, "test middleware connection should tested in the main server_test.go")
}
